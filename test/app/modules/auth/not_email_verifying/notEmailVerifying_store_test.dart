import 'package:flutter_firebase_white_label/app/modules/auth/sub_modules/not_email_verifying/controllers/notEmailVerifying_store.dart';
import 'package:flutter_test/flutter_test.dart';
 
void main() {
  late NotEmailVerifyingStore store;

  setUpAll(() {
    store = NotEmailVerifyingStore();
  });

  /*test('increment count', () async {
    expect(store.value, equals(0));
    store.increment();
    expect(store.value, equals(1));
  });*/
}